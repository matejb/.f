
(add-hook 'js2-mode-hook
          (lambda ()
            (setq indent-tabs-mode nil)))
(add-hook 'web-mode-hook
          (lambda ()
            (setq indent-tabs-mode nil)))
(add-hook 'typescript-mode-hook
          (lambda ()
            (setq indent-tabs-mode nil)))
